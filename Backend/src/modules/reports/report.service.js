import { analyticsService } from '../analytics/analytics.service.js';
import { reportRepository } from './report.repository.js';
function csv(rows) { if (!rows.length) return ''; const headers = Object.keys(rows[0]); const escape = (value) => `"${String(value ?? '').replaceAll('"', '""')}"`; return [headers.join(','), ...rows.map((row) => headers.map((key) => escape(row[key])).join(','))].join('\n'); }
export const reportService = { csv, async get(type, tenantId, query) { const range = analyticsService.range(query); if (type === 'sales') return reportRepository.sales(tenantId, range); if (type === 'orders') return reportRepository.orders(tenantId, range); if (type === 'products') return reportRepository.products(tenantId, range); if (type === 'inventory') return reportRepository.inventory(tenantId); if (type === 'payments') return reportRepository.payments(tenantId, range); if (type === 'returns') return reportRepository.returns(tenantId, range); return reportRepository.taxes(tenantId, range); } };
